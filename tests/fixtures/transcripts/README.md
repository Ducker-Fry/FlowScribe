# Transcript Search Fixtures

These JSON files are small, deterministic transcript samples for search regression tests.
They cover repeated Chinese keywords, mixed Chinese/English terms, legacy transcripts
without word timings, aligned `raw_words` plus `words`, longer transcripts, time-window
filtering, and keywords that span multiple word entries.
