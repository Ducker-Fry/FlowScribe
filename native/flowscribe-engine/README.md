# FlowScribe Engine

Native persistent `whisper.cpp` engine for FlowScribe.

Current status:

- project skeleton only
- builds an empty `flowscribe-engine.exe`
- `whisper.cpp` integration not wired yet

See `docs/whispercpp-engine-plan.md` for the implementation roadmap.

